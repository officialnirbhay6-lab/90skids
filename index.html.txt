<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>90skidsdigital</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: black;
      color: white;
      overflow-x: hidden;
    }

    /* GOLD TEXT */
    .gold {
      background: linear-gradient(45deg, #d4af37, #ffd700, #b8962e);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* INTRO */
    #loader {
      position: fixed;
      width: 100%;
      height: 100%;
      background: black;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      z-index: 999;
    }

    #loader img {
      width: 150px;
      animation: scaleUp 1s ease;
    }

    #loader h1 {
      margin-top: 10px;
      font-size: 24px;
      color: #d4af37;
      animation: fadeUp 1.5s ease;
    }

    @keyframes scaleUp {
      from { transform: scale(0.5); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px);}
      to { opacity: 1; transform: translateY(0);}
    }

    /* HERO */
    #hero {
      height: 100vh;
      position: relative;
    }

    canvas {
      display: block;
    }

    .hero-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
    }

    .hero-text h1 {
      font-size: 48px;
    }

    .btn {
      margin-top: 20px;
      padding: 12px 24px;
      border: 1px solid #d4af37;
      color: #d4af37;
      text-decoration: none;
      display: inline-block;
      border-radius: 30px;
    }

    /* SECTION */
    section {
      padding: 80px 20px;
      text-align: center;
    }

    img {
      max-width: 100%;
      border-radius: 10px;
    }

    iframe {
      width: 90%;
      max-width: 600px;
      height: 315px;
      margin: 20px 0;
      border-radius: 10px;
    }

    /* WHATSAPP */
    .whatsapp {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #25D366;
      padding: 15px;
      border-radius: 50%;
      text-decoration: none;
      color: white;
      font-size: 20px;
    }
  </style>
</head>

<body>

<!-- LOADER -->
<div id="loader">
  <img src="logo.png" />
  <h1>Nirbhay</h1>
</div>

<!-- HERO -->
<div id="hero">
  <div class="hero-text">
    <h1 class="gold">90skidsdigital</h1>
    <p>We Build Brands. We Shoot Stories.</p>
    <a href="#work" class="btn">View Work</a>
  </div>
</div>

<!-- WORK -->
<section id="work">
  <h2 class="gold">Our Work</h2>

  <iframe src="https://www.youtube.com/embed/H_5fjcyPxB8"></iframe>
  <iframe src="https://www.youtube.com/embed/ZDlPN9cPb28"></iframe>
</section>

<!-- FOUNDER -->
<section>
  <h2 class="gold">Founder</h2>
  <img src="founder.jpg">
  <p>Nirbhay — Founder of 90skidsdigital</p>
</section>

<!-- STUDIO -->
<section>
  <h2 class="gold">Studio Setup</h2>
  <img src="studio.jpg">
</section>

<!-- CONTACT -->
<section>
  <h2 class="gold">Let's Work Together</h2>
  <p>Websites • Marketing • Production</p>
</section>

<!-- WHATSAPP -->
<a class="whatsapp" href="https://wa.me/91" target="_blank">💬</a>

<!-- THREE JS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

<script>
  // REMOVE LOADER
  setTimeout(() => {
    document.getElementById("loader").style.display = "none";
  }, 2000);

  // THREE BACKGROUND
  const scene = new THREE.Scene();
  const camera = new THREE.Camera();
  const renderer = new THREE.WebGLRenderer();
  renderer.setSize(window.innerWidth, window.innerHeight);
  document.getElementById("hero").appendChild(renderer.domElement);

  const geometry = new THREE.PlaneGeometry(2, 2);

  const material = new THREE.ShaderMaterial({
    uniforms: {
      time: { value: 1.0 },
      resolution: { value: new THREE.Vector2() }
    },
    fragmentShader: `
      uniform float time;
      void main() {
        vec2 uv = gl_FragCoord.xy / vec2(800.0,600.0);
        float color = 0.0;
        color += sin(uv.x * 10.0 + time) * 0.5;
        color += sin(uv.y * 10.0 + time) * 0.5;
        gl_FragColor = vec4(vec3(color),1.0);
      }
    `
  });

  const mesh = new THREE.Mesh(geometry, material);
  scene.add(mesh);

  function animate() {
    requestAnimationFrame(animate);
    material.uniforms.time.value += 0.05;
    renderer.render(scene, camera);
  }

  animate();
</script>

</body>
</html>
